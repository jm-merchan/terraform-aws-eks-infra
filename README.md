> ⚠️ AI-Generated Module / Generated by Bob, an AI assistant powered by IBM watsonx

# eks-infra-vcs

VCS-driven HCP Terraform workspace that provisions an EKS cluster using the
private registry module `jose-merchan/eks-cluster/aws`.

This workspace is **infrastructure only** — no application workloads.
The `eks-app-vcs` workspace reads this workspace's outputs via `tfe_outputs`
and deploys NGINX onto the cluster. It is triggered automatically via a
**run trigger** whenever this workspace produces a successful apply.

## Outputs consumed by eks-app-vcs

| Output | Description |
|---|---|
| `cluster_name` | EKS cluster name |
| `cluster_endpoint` | Kubernetes API server URL |
| `cluster_certificate_authority_data` | Base64 CA cert |
| `cluster_version` | Kubernetes version |
| `oidc_provider_arn` | OIDC provider ARN for IRSA |
| `oidc_provider` | OIDC provider URL |
| `kms_key_arn` | KMS key ARN |
| `aws_region` | AWS region |
